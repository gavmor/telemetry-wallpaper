{
  "id": "telemetry-collector",
  "name": "Telemetry Collector",
  "description": "Real-time token usage telemetry collector and SVG renderer.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "spikeThreshold": {
        "type": "number",
        "default": 50000
      },
      "resolution": {
        "type": "string",
        "default": "1920x1080"
      },
      "theme": {
        "type": "string",
        "default": "gruvbox-dark"
      }
    }
  }
}
